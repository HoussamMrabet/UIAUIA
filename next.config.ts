import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['media.tenor.com', 'media1.tenor.com'], // Add other domains here if needed
  },
  /* config options here */
};

export default nextConfig;
